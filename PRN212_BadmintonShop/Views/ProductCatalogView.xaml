<UserControl x:Class="PRN212_BadmintonShop.Views.ProductCatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="16">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="icProducts">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="8" Width="200" Padding="16" Background="{DynamicResource MaterialDesignPaper}">
                            <StackPanel>
                                <TextBlock Text="{Binding Category.CategoryName}" FontSize="12" Foreground="Gray" Margin="0,0,0,4" />
                                <TextBlock Text="{Binding ProductName}" FontSize="16" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,4" />
                                <TextBlock Text="{Binding Brand}" FontSize="14" FontStyle="Italic" Margin="0,0,0,16" />
                                <TextBlock Text="{Binding Price, StringFormat=C0}" FontSize="18" FontWeight="Medium" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,0,16" />
                                
                                <Button Content="Add to Cart" Click="BtnAddToCart_Click" Style="{StaticResource MaterialDesignOutlinedButton}" />
                            </StackPanel>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
