<UserControl x:Class="PRN212_BadmintonShop.Views.CustomerHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="My Order History" FontSize="20" FontWeight="Bold" Margin="0,0,0,8" />
        
        <DataGrid x:Name="dgOrders" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="0,0,0,16">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Order ID" Binding="{Binding OrderId}" Width="80"/>
                <DataGridTextColumn Header="Date" Binding="{Binding OrderDate, StringFormat=g}" Width="150"/>
                <DataGridTextColumn Header="Status" Binding="{Binding OrderStatus.StatusName}" Width="100"/>
                <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat={}{0:C}}" Width="120"/>
                <DataGridTextColumn Header="Cancel Reason" Binding="{Binding CancelReason}" Width="*"/>
            </DataGrid.Columns>
            <!-- Expandable Row Details -->
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Border Margin="16" Padding="16" Background="#F5F5F5" BorderBrush="LightGray" BorderThickness="1" CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="Order Items" FontWeight="Bold" Margin="0,0,0,8" />
                            <DataGrid ItemsSource="{Binding OrderItems}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" Background="White">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Item" Binding="{Binding Product.ProductName}" Width="*"/>
                                    <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60"/>
                                    <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat={}{0:C}}" Width="100"/>
                                    <DataGridTextColumn Header="Total" Binding="{Binding TotalPrice, StringFormat={}{0:C}}" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="My Service Requests" FontSize="20" FontWeight="Bold" Margin="0,0,0,8" />
        
        <DataGrid x:Name="dgServices" Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Service ID" Binding="{Binding ServiceRequestId}" Width="80"/>
                <DataGridTextColumn Header="Type" Binding="{Binding ServiceType.TypeName}" Width="120"/>
                <DataGridTextColumn Header="Request Date" Binding="{Binding RequestedDate, StringFormat=g}" Width="150"/>
                <DataGridTextColumn Header="Status" Binding="{Binding ServiceStatus.StatusName}" Width="100"/>
                <DataGridTextColumn Header="Estimated Return" Binding="{Binding EstReturnDate, StringFormat=g}" Width="150"/>
                <DataGridTextColumn Header="Cancel Reason" Binding="{Binding CancelReason}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
