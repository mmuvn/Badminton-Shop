<UserControl x:Class="PRN212_BadmintonShop.Views.ManageProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Manage Products" FontSize="24" FontWeight="Bold" />
            <Button Content="Add New Product" HorizontalAlignment="Right" Click="BtnAddProduct_Click" Style="{StaticResource MaterialDesignFlatDarkBgButton}" />
        </Grid>

        <DataGrid x:Name="dgProducts" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                  CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProductId}" />
                <DataGridTextColumn Header="Name" Binding="{Binding ProductName}" />
                <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category.CategoryName}" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C0}" />
                <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" />
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" />
                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Click="BtnEditProduct_Click" Margin="0,0,8,0" Style="{StaticResource MaterialDesignOutlinedButton}" />
                                <Button Content="Toggle Active" Click="BtnToggleActive_Click" Style="{StaticResource MaterialDesignOutlinedButton}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
