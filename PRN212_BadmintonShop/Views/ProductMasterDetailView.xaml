<UserControl x:Class="PRN212_BadmintonShop.Views.ProductMasterDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>

        <!-- TOP BAR (Search & Cart) -->
        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="8,8,8,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <TextBox x:Name="txtSearch" Grid.Column="1" VerticalAlignment="Center" TextChanged="TxtSearch_TextChanged" />
            <Button Grid.Column="2" Content="Clear" Margin="8,0,0,0" Click="BtnClearSearch_Click" Style="{StaticResource MaterialDesignOutlinedButton}" />

            <Button x:Name="btnViewCart" Grid.Column="4" Content="View Cart (0)" Click="BtnViewCart_Click" 
                    Visibility="{Binding IsCustomer, Converter={StaticResource BooleanToVisibilityConverter}}" 
                    Style="{StaticResource MaterialDesignOutlinedButton}" />
        </Grid>

        <!-- LEFT PANEL (DataGrid) -->
        <Border Grid.Row="1" Grid.Column="0" BorderBrush="CornflowerBlue" BorderThickness="2" Margin="8,0,0,0">
            <DataGrid x:Name="dgProducts" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                      SelectionChanged="DgProducts_SelectionChanged" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ProductId}" Width="50" />
                    <DataGridTextColumn Header="Title" Binding="{Binding ProductName}" Width="*" />
                    <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" Width="150" />
                    <DataGridTextColumn Header="Category" Binding="{Binding Category.CategoryName}" Width="100" />
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat={}{0:C}}" Width="100" />
                    <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}" Width="80" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- SPLITTER -->
        <GridSplitter Grid.Row="1" Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent" />

        <!-- RIGHT PANEL (Details Form) -->
        <Border Grid.Row="1" Grid.Column="2" BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Margin="0,0,8,0" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Product Details" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" />

                <!-- Form ScrollViewer -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="pnlForm">
                        
                        <!-- Base Fields -->
                        <TextBlock Text="Category *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <ComboBox x:Name="cbCategory" DisplayMemberPath="CategoryName" SelectedValuePath="CategoryId" 
                                  SelectionChanged="CbCategory_SelectionChanged" Margin="0,0,0,16" />

                        <TextBlock Text="Title *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="txtTitle" Margin="0,0,0,16" />

                        <TextBlock Text="Brand *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="txtBrand" Margin="0,0,0,16" />

                        <TextBlock Text="Price *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="txtPrice" Margin="0,0,0,16" />

                        <TextBlock Text="Stock *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="txtStock" Margin="0,0,0,16" />

                        <TextBlock Text="Description" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="txtDescription" AcceptsReturn="True" TextWrapping="Wrap" MaxHeight="80" Margin="0,0,0,16" />

                        <!-- Dynamic Specific Fields (Generated in CodeBehind or controlled via visibility) -->
                        <Separator Margin="0,0,0,16" />
                        <TextBlock Text="Specifications" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8" />
                        
                        <StackPanel x:Name="pnlDynamicFields">
                            <!-- Injected from C# based on Category -->
                        </StackPanel>

                    </StackPanel>
                </ScrollViewer>

                <!-- Action Buttons (Bottom of Form) -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    
                    <!-- Customer Quantity & Add -->
                    <StackPanel x:Name="pnlCustomerActions" Orientation="Horizontal" Visibility="{Binding IsCustomer, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="Qty:" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBox x:Name="txtQuantity" Text="1" Width="40" Margin="0,0,16,0" HorizontalContentAlignment="Center" VerticalAlignment="Center" />
                    </StackPanel>

                    <Button x:Name="btnClearForm" Content="Clear" Click="BtnClearForm_Click" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0" />
                    <Button x:Name="btnSave" Content="Save" Click="BtnSave_Click" Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource MaterialDesignFlatDarkBgButton}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- BOTTOM BAR (List Actions) -->
        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="8,8,8,8">
            <Button x:Name="btnAdd" Content="+ Add New" Click="BtnAdd_Click" Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0" />
            <Button x:Name="btnDelete" Content="Delete" Click="BtnDelete_Click" Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0" />
            <Button x:Name="btnAddToCartList" Content="Add to Cart" Click="BtnAddToCartList_Click" Visibility="{Binding IsCustomer, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,8,0" />
            
            <Button x:Name="btnLogout" Content="Logout" Click="BtnLogout_Click" Style="{StaticResource MaterialDesignOutlinedButton}" />
        </StackPanel>
    </Grid>
</UserControl>
