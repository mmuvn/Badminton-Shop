<UserControl x:Class="PRN212_BadmintonShop.Views.CheckoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Checkout" FontSize="24" FontWeight="Bold" Margin="0,0,0,16"/>

        <DataGrid x:Name="dgCheckoutItems" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="0,0,0,16">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="60"/>
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat={}{0:C}}" Width="120"/>
                <DataGridTextColumn Header="Line Total" Binding="{Binding LineTotal, StringFormat={}{0:C}}" Width="120"/>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Remove" Click="BtnRemove_Click" Tag="{Binding}" Style="{StaticResource MaterialDesignOutlinedButton}" Padding="8,0" Height="24" Foreground="IndianRed" BorderBrush="IndianRed" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="8" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,24,0">
                    <TextBlock Text="Total:" FontSize="20" FontWeight="Bold" Margin="0,0,8,0"/>
                    <TextBlock x:Name="txtTotal" Text="0₫" FontSize="24" Foreground="{DynamicResource PrimaryHueMidBrush}" FontWeight="Bold"/>
                </StackPanel>

                <Button Grid.Column="2" Content="Pay Now" Click="BtnPay_Click" Style="{StaticResource MaterialDesignFlatDarkBgButton}" Padding="24,8" Height="45" FontSize="16" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
