<Window x:Class="PRN212_BadmintonShop.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Login - Badminton Shop" Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome Back" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,32" />

        <TextBox x:Name="txtUsername" Grid.Row="1"
                 materialDesign:HintAssist.Hint="Username"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                 Margin="0,0,0,16" />

        <PasswordBox x:Name="txtPassword" Grid.Row="2"
                     materialDesign:HintAssist.Hint="Password"
                     Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"
                     Margin="0,0,0,24" />

        <Button x:Name="btnLogin" Grid.Row="3" Content="LOGIN" Click="BtnLogin_Click" />

        <TextBlock x:Name="txtError" Grid.Row="4" Foreground="Red" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,16,0,0" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Don't have an account? " VerticalAlignment="Center"/>
            <Button Content="Register" Style="{StaticResource MaterialDesignFlatButton}" Click="BtnRegister_Click" />
        </StackPanel>
    </Grid>
</Window>
