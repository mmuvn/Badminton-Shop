<Window x:Class="PRN212_BadmintonShop.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Register - Badminton Shop" Height="600" Width="400"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Create Account" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,24" />

        <TextBox x:Name="txtFullName" Grid.Row="1" materialDesign:HintAssist.Hint="Full Name" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16" />
        <TextBox x:Name="txtUsername" Grid.Row="2" materialDesign:HintAssist.Hint="Username" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16" />
        <TextBox x:Name="txtEmail" Grid.Row="3" materialDesign:HintAssist.Hint="Email" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16" />
        <PasswordBox x:Name="txtPassword" Grid.Row="4" materialDesign:HintAssist.Hint="Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Margin="0,0,0,16" />
        <TextBox x:Name="txtPhone" Grid.Row="5" materialDesign:HintAssist.Hint="Phone Number" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,16" />
        <TextBox x:Name="txtAddress" Grid.Row="6" materialDesign:HintAssist.Hint="Address" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,0,24" />

        <Button x:Name="btnRegister" Grid.Row="7" Content="REGISTER" Click="BtnRegister_Click" />

        <TextBlock x:Name="txtError" Grid.Row="8" Foreground="Red" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,16,0,0" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Already have an account? " VerticalAlignment="Center"/>
            <Button Content="Login" Style="{StaticResource MaterialDesignFlatButton}" Click="BtnLogin_Click" />
        </StackPanel>
    </Grid>
</Window>
